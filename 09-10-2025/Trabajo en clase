Trabajo en clase

; es operador sincronico. No se ejecuta c hasta ejecutado b, y b hasta ejecutado a

> print = console.log
[Function: log]
> print('a');print('b');print('c')
a
b
c
undefined
> 

timers

setTimeout(lambda que quiero que se ejecute)

> setTimeout(() => print('g de Honduras!!!'), 5000); print('a lo que vinimos')
a lo que vinimos
undefined
> g de Honduras!!!

setInterval = repetir lambda n segundos

> setInterval(() => print('gol de Honduras'), 5000); print('a lo que vinimos')
a lo que vinimos
undefined
> gol de Honduras
gol de Honduras



