// Importando la función sum del módulo test.mjs
import { sum, sum_2, sum_3 } from "./test.mjs";

const a = [1, 2, 3, 4, -1];

console.log(`sum(a) = ${sum(a)}`);

console.log(`sum_2(a) = ${sum_2(a)}`);

console.log(`sum_3(a) = ${sum_3(a)}`);
